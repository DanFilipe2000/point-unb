<!DOCTYPE html>
<html>
<head>
    <title>Feed de Imagens</title>
    <style>
        h1{
            color:aliceblue;

        }

        span{
            padding: 5px;
            border-radius: 100px;
            background-color: #a872cc;
            
           
        }

        p{
            background-color: rgb(212, 58, 58);
            padding: 10px;
            border-radius: 20px;
            color: white;
        }

        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #0c0b0b;
            margin: 10px 10px;
        }

        header {
            background-color: #9e0000;
            border-bottom: 1px solid #dbdbdb;
            padding: 10px 0;
            text-align: center;
        }

        header img {
            width: 100px;
            height: auto;
        }

        .feed-container {
            display: grid;
            grid-template-columns: repeat(1fr);
            gap: 16px;
            max-width: 700px;
            margin: 20px auto;
            padding: 20px;
        }

        .image-post {
            position: relative;
            overflow: hidden;
            border: 1px solid #dbdbdb;
            border-radius: 8px;
        }

        .image-post img {
            display: block;
            width: 100%;
            height: auto;
        }

        .image-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.3);
            display: flex;
            justify-content: center;
            align-items: center;
            opacity: 0;
            transition: opacity 0.3s ease-in-out;
        }

        .image-post:hover .image-overlay {
            opacity: 1;
        }

        .overlay-text {
            color: white;
            font-size: 1.2rem;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <p>
        <span>
        <a href="tela_registro.ejs">Login</a></span>⠀⠀⠀⠀⠀ 
        <span>
        <a href="cadastrarLugar.ejs">Registrar</a></span>
        </span>
    </p>
    <div class="feed-container">
        <% dados.forEach(function(item) { %>
            <div class="image-post">
                <h1><%= item.nome %></h1>
                <img src="<%= '../' + item.foto %>" alt="Descrição da imagem">
                <p>Nota: <%= item.nota %></p>
                <p>Descrição: <%= item.descricao %></p>
            </div>
        <% }); %>
    </div>
</body>
</html>
